# Variables lues par docker-compose.yml / docker-compose.prod.yml (docker compose charge
# automatiquement un fichier ".env" placé à la racine). Copier en ".env" et compléter.

APP_KEY=

# Supervision Sentry — https://sentry.io (1 projet Laravel + 1 projet React, gratuits)
SENTRY_LARAVEL_DSN=
VITE_SENTRY_DSN=
